# Variables de entorno necesarias para el proyecto

# Base de datos MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password
DB_PORT=3306
DB_NAME=firmas_digitales_v2

# JWT Secret para autenticación (genera uno seguro)
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui

# Clave para cifrado de llaves privadas (debe ser exactamente 32 caracteres)
KEY_ENCRYPTION_SECRET=12345678901234567890123456789012

# Entorno (development/production)
NODE_ENV=development

# Puerto (opcional, Railway lo asigna automáticamente en producción)
PORT=3000
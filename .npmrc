# Configuración de npm para usar la nueva sintaxis recomendada
# En lugar de --production, usar --omit=dev
omit=dev
# Configuración adicional para optimizar instalaciones
audit=false
fund=false
save-exact=true